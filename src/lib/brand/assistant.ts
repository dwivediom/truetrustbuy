/** Human-facing specialist name for product chat (search + seller knowledge). */
export const ASSISTANT_NAME = "Jiya";

/** Max guest user messages to /api/seller/bot/respond before sign-in is required. */
export const MAX_GUEST_PRODUCT_CHAT_TURNS = 4;
